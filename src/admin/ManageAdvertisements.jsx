import {useEffect,useState} from 'react';
import {Link} from 'react-router-dom';
import {HiPlus,HiPencil,HiTrash} from 'react-icons/hi';
import toast from 'react-hot-toast';
import {api,errorDetails} from '../lib/api';
const statusOf=a=>{if(!a.enabled)return['Disabled','bg-dark-700 text-dark-300'];const now=Date.now();if(a.startDate&&new Date(a.startDate)>now)return['Scheduled','bg-blue-500/15 text-blue-300'];if(a.endDate&&new Date(a.endDate)<now)return['Expired','bg-amber-500/15 text-amber-300'];return['Active','bg-green-500/15 text-green-300']};
export default function ManageAdvertisements(){const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[busy,setBusy]=useState('');useEffect(()=>{api.get('/admin/advertisements').then(r=>setItems(r.data)).catch(e=>toast.error(errorDetails(e).summary)).finally(()=>setLoading(false))},[]);const remove=async a=>{if(!confirm(`Delete "${a.title||'Untitled advertisement'}" and its uploaded image?`))return;setBusy(a.id);try{await api.delete('/admin/advertisements/'+a.id);setItems(v=>v.filter(x=>x.id!==a.id));toast.success('Advertisement deleted')}catch(e){toast.error(errorDetails(e).summary)}finally{setBusy('')}};return <div className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-3xl font-bold text-white">Advertisements</h1><p className="mt-1 text-dark-400">Manage promotional slides in the homepage hero.</p></div><Link to="/admin/advertisements/new" className="admin-btn-primary flex items-center justify-center gap-2"><HiPlus/>Add Advertisement</Link></header>{loading?<div className="admin-card text-dark-300">Loading advertisements...</div>:items.length?<div className="grid gap-4 lg:grid-cols-2">{items.map(a=>{const status=statusOf(a);return <article key={a.id} className="admin-card overflow-hidden p-0"><div className="aspect-[16/10] bg-slate-900">{a.imageUrl?<img src={a.imageUrl} alt="" className={`h-full w-full ${a.fitMode==='cover'?'object-cover':'object-contain'}`} style={{objectPosition:`${a.positionX||'center'} ${a.positionY||'center'}`}}/>:<div className="grid h-full place-items-center text-dark-500">No image</div>}</div><div className="p-5"><div className="flex justify-between gap-3"><div><h2 className="font-bold text-white">{a.title||'Untitled advertisement'}</h2><p className="mt-1 line-clamp-2 text-sm text-dark-300">{a.description||'No description'}</p></div><span className={`h-fit rounded-full px-3 py-1 text-xs font-semibold ${status[1]}`}>{status[0]}</span></div><div className="mt-3 text-xs text-dark-500">Order {a.displayOrder} - {a.fitMode||'contain'} - {(a.positionX||'center')}/{(a.positionY||'center')}</div><div className="mt-4 grid grid-cols-2 gap-3"><Link to={'/admin/advertisements/edit/'+a.id} className="admin-btn-secondary flex justify-center gap-2"><HiPencil/>Edit</Link><button disabled={busy===a.id} onClick={()=>remove(a)} className="admin-btn-danger flex justify-center gap-2"><HiTrash/>{busy===a.id?'Deleting...':'Delete'}</button></div></div></article>})}</div>:<div className="admin-card py-14 text-center text-dark-400">No advertisements yet. The homepage will continue showing the terminal visual.</div>}</div>}
