import {createContext,useContext,useEffect,useState} from 'react';import {api} from '../lib/api';
const CatalogContext=createContext();
const normalize=x=>({...x,category:x.category.name,spec:x.shortDescription,imageUrl:x.imageUrl||'/favicon.svg',price:Number(x.price),inStock:x.stockStatus!=='OUT_OF_STOCK'&&(x.stockQuantity>0||x.stockStatus==='TO_ORDER'),stockStatus:x.stockStatus.replaceAll('_',' ').toLowerCase(),compatibilityType:x.compatibilityType||x.category.slug});
export function CatalogProvider({children}){const [products,setProducts]=useState([]),[packages,setPackages]=useState([]),[rules,setRules]=useState([]),[erpFeatures,setErpFeatures]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{Promise.all([api.get('/products',{params:{limit:48}}),api.get('/software-packages'),api.get('/compatibility-rules'),api.get('/erp-key-features')]).then(([p,s,r,e])=>{setProducts(p.data.items.map(normalize));setPackages(s.data.map(x=>({id:x.slug,databaseId:x.id,name:x.name,price:Number(x.startingPrice).toLocaleString('en-US'),description:x.suitableFor,keyFeatures:x.features.filter(f=>f.featured).map(f=>f.text),features:x.features.map(f=>f.text),optionalAddOns:x.addons.map(a=>a.text),supportPeriod:x.supportPeriod,note:x.licenceNote,badge:x.mostPopular?'Most Popular':null})));setRules(r.data);setErpFeatures(e.data)}).finally(()=>setLoading(false))},[]);return <CatalogContext.Provider value={{products,packages,rules,erpFeatures,loading}}>{children}</CatalogContext.Provider>}export const useCatalog=()=>useContext(CatalogContext);
