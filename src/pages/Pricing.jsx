import {motion} from 'framer-motion';
import {Link} from 'react-router-dom';
import {useData} from '../context/DataContext';
export default function Pricing(){
 const {solutions,loading}=useData(),priced=solutions.filter(s=>s.packages?.length);
 return <><section className="relative overflow-hidden pb-10 pt-32"><div className="absolute inset-0 bg-gradient-to-b from-dark-50 to-white dark:from-dark-950 dark:to-dark-950"/><div className="relative z-10 mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><span className="section-subtitle">Pricing</span><h1 className="section-title mt-4">Pricing by <span className="gradient-text">Solution</span></h1><p className="mt-4 max-w-xl text-dark-500 dark:text-dark-300">Choose a solution to view its current packages and starting prices.</p></div></section><section className="px-4 pb-24 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-6xl gap-5 sm:grid-cols-2 lg:grid-cols-3">{priced.map((sol,i)=><motion.div key={sol.id} initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{delay:i*.07}}><Link to={'/solutions/'+sol.id+'#pricing'} className="glass-card-hover flex h-full flex-col p-6"><h2 className="text-lg font-bold text-dark-900 dark:text-white">{sol.title}</h2><p className="mt-2 text-sm text-dark-500 dark:text-dark-400">{sol.shortTitle||sol.description}</p><div className="mt-4 flex flex-wrap gap-2">{sol.packages.map(p=><span key={p.id} className="pill text-xs">{p.name}</span>)}</div><div className="mt-6 border-t border-dark-200 pt-4 dark:border-dark-700"><span className="text-xs text-dark-400">Starting from</span><div className="gradient-text text-xl font-extrabold">LKR {Math.min(...sol.packages.map(p=>Number(p.startingPrice))).toLocaleString('en-US')}</div></div></Link></motion.div>)}{!loading&&!priced.length&&<p className="col-span-full py-16 text-center text-dark-400">No pricing packages are currently available.</p>}</div></section></>
}
